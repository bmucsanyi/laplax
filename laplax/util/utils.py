"""General utility functions."""

from laplax.types import Any


def identity(x: Any) -> Any:
    """Identity function.

    Args:
        x: Any input.

    Returns:
        The input itself.
    """
    return x
